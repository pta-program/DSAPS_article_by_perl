## 6-12 二叉搜索树的操作集

- **分值：** 30分

## 题目描述

本题要求实现给定二叉搜索树的5种常用操作。

## 函数接口定义
```perl
# Perl 用哈希/数组引用模拟题目中的结点和容器类型。
use strict;
use warnings;

sub new_node {
    my ( $data, $left, $right ) = @_;
    return { data => $data, left => $left, right => $right };
}
sub insert;
sub delete;
sub find;
sub find_min;
sub find_max;
```

其中`BinTree`结构定义如下：

```perl
# BinTree 和 Position 都使用二叉树结点哈希引用；空指针用 undef 表示。
```

- 函数`Insert`将`X`插入二叉搜索树`BST`并返回结果树的根结点指针；
- 函数`Delete`将`X`从二叉搜索树`BST`中删除，并返回结果树的根结点指针；如果`X`不在树中，则打印一行`Not Found`并返回原树的根结点指针；
- 函数`Find`在二叉搜索树`BST`中找到`X`，返回该结点的指针；如果找不到则返回空指针；
- 函数`FindMin`返回二叉搜索树`BST`中最小元结点的指针；
- 函数`FindMax`返回二叉搜索树`BST`中最大元结点的指针。

## 裁判测试程序样例
```perl
# 裁判样例：读取三组操作，按原题格式调用 BST 接口。
use strict;
use warnings;

sub preorder_traversal {
    my ($root) = @_;
    return unless $root;
    print " $root->{data}";
    preorder_traversal( $root->{left} );
    preorder_traversal( $root->{right} );
}

sub inorder_traversal {
    my ($root) = @_;
    return unless $root;
    inorder_traversal( $root->{left} );
    print " $root->{data}";
    inorder_traversal( $root->{right} );
}

sub main {
    my @input = split /\s+/, join '', <STDIN>;
    return unless @input;
    my $at = 0;
    my $n = $input[ $at++ ];
    my $tree;
    $tree = insert( $tree, $input[ $at++ ] ) for 1 .. $n;
    print 'Preorder:';
    preorder_traversal($tree);
    print "\n";

    my $min = find_min($tree);
    my $max = find_max($tree);
    $n = $input[ $at++ ];
    for ( 1 .. $n ) {
        my $value = $input[ $at++ ];
        my $found = find( $tree, $value );
        if ($found) {
            print "$found->{data} is found\n";
            print "$found->{data} is the smallest key\n" if $found == $min;
            print "$found->{data} is the largest key\n"  if $found == $max;
        }
        else { print "$value is not found\n" }
    }

    $n = $input[ $at++ ];
    $tree = &delete( $tree, $input[ $at++ ] ) for 1 .. $n;
    print 'Inorder:';
    inorder_traversal($tree);
    print "\n";
}
main();
```

## 输入样例
```text
10
5 8 6 2 4 1 0 10 9 7
5
6 3 10 0 5
5
5 7 0 10 3
```

## 输出样例
```text
Preorder: 5 2 1 0 4 8 6 7 10 9
6 is found
3 is not found
10 is found
10 is the largest key
0 is found
0 is the smallest key
5 is found
Not Found
Inorder: 1 2 4 6 8 9
```

## 函数部分

```perl
# 查找/求极值沿单侧路径完成；删除双孩子结点时用右子树最小值替代。
sub find {
    my ( $root, $value ) = @_;
    while ( $root && $root->{data} != $value ) {
        $root = $value < $root->{data} ? $root->{left} : $root->{right};
    }
    return $root;
}

sub find_min {
    my ($root) = @_;
    $root = $root->{left} while $root && $root->{left};
    return $root;
}

sub find_max {
    my ($root) = @_;
    $root = $root->{right} while $root && $root->{right};
    return $root;
}

sub insert {
    my ( $root, $value ) = @_;
    return new_node( $value, undef, undef ) unless $root;
    if ( $value < $root->{data} ) {
        $root->{left} = insert( $root->{left}, $value );
    }
    elsif ( $value > $root->{data} ) {
        $root->{right} = insert( $root->{right}, $value );
    }
    return $root;
}

sub delete {
    my ( $root, $value ) = @_;
    unless ( find( $root, $value ) ) {
        print "Not Found\n";
        return $root;
    }
    return _delete_node( $root, $value );
}

sub _delete_node {
    my ( $root, $value ) = @_;
    if ( $value < $root->{data} ) {
        $root->{left} = _delete_node( $root->{left}, $value );
    }
    elsif ( $value > $root->{data} ) {
        $root->{right} = _delete_node( $root->{right}, $value );
    }
    elsif ( !$root->{left} )  { return $root->{right} }
    elsif ( !$root->{right} ) { return $root->{left} }
    else {
        my $successor = find_min( $root->{right} );
        $root->{data}  = $successor->{data};
        $root->{right} = _delete_node( $root->{right}, $successor->{data} );
    }
    return $root;
}
```

## 解题思路

二叉搜索树满足左子树所有关键字小于根、右子树所有关键字大于根，因此查找、插入、求最小值和求最大值都可以沿一条路径完成。删除时分三种情况：叶结点直接删除；只有一个孩子时让孩子顶替当前结点；有两个孩子时使用右子树的最小结点（中序后继）替换当前数据，再删除这个后继。插入遇到重复值时不重复建立结点。平均时间复杂度为 `O(log n)`，退化时为 `O(n)`。

## 代码流程说明

1. `Find` 比较 `X` 与当前结点，按大小递归进入左子树或右子树。
2. `FindMin` 沿左指针走到尽头，`FindMax` 沿右指针走到尽头。
3. `Insert` 找到空位置后申请新结点，并将递归结果重新接回父结点。
4. `Delete` 先定位目标；无孩子或只有一个孩子时直接调整指针，有两个孩子时复制后继数据并删除后继。
5. 使用 `found` 标记删除是否成功，避免目标不存在时误删或丢失原树结构。

## 代码流程图

```mermaid
flowchart TD
    A[调用 BST 操作] --> B{操作类型}
    B -- Find --> C[比较 X 与当前结点]
    C --> D{相等}
    D -- 是 --> E[返回当前结点]
    D -- 否 --> F[按大小进入左或右子树]
    B -- FindMin --> G[沿左指针向下]
    B -- FindMax --> H[沿右指针向下]
    B -- Insert --> I{当前位置为空}
    I -- 是 --> J[创建新结点]
    I -- 否 --> K[按大小递归插入]
    B -- Delete --> L{找到目标结点}
    L -- 否 --> M[输出 Not Found, 保留原树]
    L -- 是 --> N{有几个孩子}
    N -- 两个 --> O[用右子树最小值替换并删除后继]
    N -- 零个或一个 --> P[用孩子替换并释放当前结点]
```

## 解题流程图

```mermaid
flowchart TD
    A[建立二叉搜索树] --> B[读取操作请求]
    B --> C[利用大小关系定位结点]
    C --> D{查找、插入、删除或求极值}
    D -- 查找 --> E[返回结点或空指针]
    D -- 插入 --> F[在空位置建立结点]
    D -- 删除 --> G[按孩子数量调整树结构]
    D -- 求极值 --> H[沿单侧路径找到最值]
    E --> I[输出或继续处理]
    F --> I
    G --> I
    H --> I
```
