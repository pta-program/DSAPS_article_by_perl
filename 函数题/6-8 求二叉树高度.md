## 6-8 求二叉树高度

- **分值：** 20分

## 题目描述

本题要求给定二叉树的高度。

## 函数接口定义
```python
from dataclasses import dataclass


@dataclass
class TreeNode:
    data: str
    left: "TreeNode | None" = None
    right: "TreeNode | None" = None


BinTree = TreeNode | None


def get_height(root: BinTree) -> int: ...
```

其中`BinTree`结构定义如下：

```python
from dataclasses import dataclass


@dataclass
class TreeNode:
    data: str
    left: "TreeNode | None" = None
    right: "TreeNode | None" = None


BinTree = TreeNode | None


def get_height(root: BinTree) -> int: ...
```

要求函数返回给定二叉树BT的高度值。

## 裁判测试程序样例
```python
def main():
    # 裁判端建立 TreeNode 根结点后调用 get_height(root)。
    pass


if __name__ == "__main__":
    main()
```

## 输出样例
![](6-8%20求二叉树高度/image/40.jpg)

```text
4
```

## 函数部分

```python
def get_height(root: BinTree) -> int:
    """空树高度为 0，非空树为左右子树高度较大值加 1。"""
    if root is None:
        return 0
    return 1 + max(get_height(root.left), get_height(root.right))
```

## 解题思路

采用递归定义树高：空树高度为 0，非空树高度等于左右子树高度的较大值加 1。递归会覆盖每个结点一次，因此时间复杂度为 `O(n)`；递归栈的空间复杂度为 `O(h)`，其中 `h` 是树高。

## 代码流程说明

1. 若当前结点为空，返回 0。
2. 递归计算左子树和右子树的高度。
3. 取两个高度中的较大值并加 1，作为当前树高返回。

## 代码流程图

```mermaid
flowchart TD
    A[调用 GetHeight(BT)] --> B{BT 是否为空}
    B -- 是 --> C[返回 0]
    B -- 否 --> D[递归求左子树高度]
    D --> E[递归求右子树高度]
    E --> F[取较大值并加一]
    F --> G[返回当前树高]
```

## 解题流程图

```mermaid
flowchart TD
    A[输入二叉树] --> B[分解为左右子树]
    B --> C{当前子树为空}
    C -- 是 --> D[高度为 0]
    C -- 否 --> E[继续递归分解]
    D --> F[比较左右高度]
    E --> F
    F --> G[加一得到当前结点高度]
```
