## 6-8 求二叉树高度

- **分值：** 20分

## 题目描述

本题要求给定二叉树的高度。

## 函数接口定义
```perl
# Perl 用哈希/数组引用模拟题目中的结点和容器类型。
use strict;
use warnings;

sub new_node {
    my ( $data, $left, $right ) = @_;
    return { data => $data, left => $left, right => $right };
}
sub get_height;
```

其中`BinTree`结构定义如下：

```perl
# BinTree 是二叉树根结点的哈希引用；字段为 data、left 和 right。
```

要求函数返回给定二叉树BT的高度值。

## 裁判测试程序样例
```perl
# 裁判样例：构造图片中的二叉树并调用高度函数。
use strict;
use warnings;

sub main {
    my $tree = new_node(
        'A',
        new_node( 'B', new_node('D'), new_node( 'F', new_node('E'), undef ) ),
        new_node( 'C', new_node( 'G', undef, new_node('H') ), new_node('I') )
    );
    print get_height($tree), "\n";
}
main();
```

## 输出样例
![](6-8%20求二叉树高度/image/40.jpg)

```text
4
```

## 函数部分

```perl
# 空树高度为 0；递归取左右子树较大高度并加 1。
sub get_height {
    my ($root) = @_;
    return 0 unless $root;
    my $left  = get_height( $root->{left} );
    my $right = get_height( $root->{right} );
    return 1 + ( $left > $right ? $left : $right );
}
```

## 解题思路

采用递归定义树高：空树高度为 0，非空树高度等于左右子树高度的较大值加 1。递归会覆盖每个结点一次，因此时间复杂度为 `O(n)`；递归栈的空间复杂度为 `O(h)`，其中 `h` 是树高。

## 代码流程说明

1. 若当前结点为空，返回 0。
2. 递归计算左子树和右子树的高度。
3. 取两个高度中的较大值并加 1，作为当前树高返回。

## 代码流程图

```mermaid
flowchart TD
    A["调用 GetHeight(BT)"] --> B{BT 是否为空}
    B -- 是 --> C[返回 0]
    B -- 否 --> D[递归求左子树高度]
    D --> E[递归求右子树高度]
    E --> F[取较大值并加一]
    F --> G[返回当前树高]
```

## 解题流程图

```mermaid
flowchart TD
    A[输入二叉树] --> B[分解为左右子树]
    B --> C{当前子树为空}
    C -- 是 --> D[高度为 0]
    C -- 否 --> E[继续递归分解]
    D --> F[比较左右高度]
    E --> F
    F --> G[加一得到当前结点高度]
```
