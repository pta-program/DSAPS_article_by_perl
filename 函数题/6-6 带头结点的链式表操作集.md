## 6-6 带头结点的链式表操作集

- **分值：** 20分

## 题目描述

本题要求实现带头结点的链式表操作集。

## 函数接口定义
```python
from dataclasses import dataclass


@dataclass
class Node:
    data: int = 0
    next: "Node | None" = None


List = Node  # 头结点始终存在
Position = Node | None


def make_empty() -> List: ...
def find(head: List, value: int) -> Position: ...
def insert(head: List, value: int, position: Position) -> bool: ...
def delete(head: List, position: Position) -> bool: ...
```

其中`List`结构定义如下：

```python
from dataclasses import dataclass


@dataclass
class Node:
    data: int = 0
    next: "Node | None" = None


List = Node  # 头结点始终存在
Position = Node | None


def make_empty() -> List: ...
def find(head: List, value: int) -> Position: ...
def insert(head: List, value: int, position: Position) -> bool: ...
def delete(head: List, position: Position) -> bool: ...
```

各个操作函数的定义为：

`List MakeEmpty()`：创建并返回一个空的线性表；

`Position Find( List L, ElementType X )`：返回线性表中X的位置。若找不到则返回ERROR；

`bool Insert( List L, ElementType X, Position P )`：将X插入在位置P指向的结点之前，返回true。如果参数P指向非法位置，则打印“Wrong Position for Insertion”，返回false；

`bool Delete( List L, Position P )`：将位置P的元素删除并返回true。若参数P指向非法位置，则打印“Wrong Position for Deletion”并返回false。

## 裁判测试程序样例
```python
def main():
    # 裁判端创建头结点表后调用四个待测函数。
    pass


if __name__ == "__main__":
    main()
```

## 输入样例
```text
6
12 2 4 87 10 2
4
2 12 87 5
```

## 输出样例
```text
2 is found and deleted.
12 is found and deleted.
87 is found and deleted.
Finding Error: 5 is not in.
5 is inserted as the last element.
Wrong Position for Insertion
Wrong Position for Deletion
10 4 2 5
```

## 函数部分

```python
def make_empty() -> List:
    """创建只包含头结点的空表。"""
    return Node()


def find(head: List, value: int) -> Position:
    current = head.next
    while current is not None:
        if current.data == value:
            return current
        current = current.next
    return None


def insert(head: List, value: int, position: Position) -> bool:
    """在 position 前插入，None 表示尾插；位置必须属于当前链表。"""
    previous = head
    if position is None:
        while previous.next is not None:
            previous = previous.next
    else:
        while previous.next is not position and previous.next is not None:
            previous = previous.next
        if previous.next is not position:
            return False
    previous.next = Node(value, position)
    return True


def delete(head: List, position: Position) -> bool:
    previous = head
    while previous.next is not position and previous.next is not None:
        previous = previous.next
    if position is None or previous.next is not position:
        return False
    previous.next = position.next
    return True
```

## 解题思路

头结点本身不存储有效数据，只负责统一处理表头位置。所有查找都从 `L->Next` 开始；插入和删除都从头结点开始寻找目标位置的前驱，因此不需要为删除首元结点额外修改表头。`P == NULL` 时按题意将元素插入表尾。

## 代码流程说明

1. `MakeEmpty` 分配头结点并令其 `Next` 为空。
2. `Find` 跳过头结点，从第一个有效结点开始查找。
3. `Insert`：指定 `P` 时寻找其前驱；`P == NULL` 时寻找最后一个结点；申请新结点并接入前驱之后。
4. `Delete` 从头结点寻找 `P` 的前驱，确认位置合法后调整链接并释放 `P`。

## 代码流程图

```mermaid
flowchart TD
    A[调用操作] --> B{操作类型}
    B -- MakeEmpty --> C[创建头结点, Next=NULL]
    B -- Find --> D[从 L->Next 开始扫描]
    D --> E{找到 X}
    E -- 是 --> F[返回结点]
    E -- 否 --> G[返回 ERROR]
    B -- Insert --> H{P 是否为空}
    H -- 是 --> I[寻找尾结点]
    H -- 否 --> J[寻找 P 的前驱]
    I --> K[申请新结点并插入]
    J --> L{前驱存在}
    L -- 否 --> M[报错并失败]
    L -- 是 --> K
    B -- Delete --> N[从头结点寻找 P 的前驱]
    N --> O{位置是否合法}
    O -- 否 --> P[报错并失败]
    O -- 是 --> Q[调整链接并释放 P]
```

## 解题流程图

```mermaid
flowchart TD
    A[创建带头结点的空表] --> B[接收操作]
    B --> C[从头结点统一定位前驱]
    C --> D{操作是否合法}
    D -- 否 --> E[输出错误信息]
    D -- 是 --> F[完成查找、插入或删除]
    E --> B
    F --> B
```
