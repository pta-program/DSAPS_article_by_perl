## 6-9 二叉树的遍历

- **分值：** 25分

## 题目描述

本题要求给定二叉树的4种遍历。

## 函数接口定义
```python
from dataclasses import dataclass


@dataclass
class TreeNode:
    data: str
    left: "TreeNode | None" = None
    right: "TreeNode | None" = None


BinTree = TreeNode | None


def inorder(root: BinTree) -> None: ...
def preorder(root: BinTree) -> None: ...
def postorder(root: BinTree) -> None: ...
def levelorder(root: BinTree) -> None: ...
```

其中`BinTree`结构定义如下：

```python
from dataclasses import dataclass


@dataclass
class TreeNode:
    data: str
    left: "TreeNode | None" = None
    right: "TreeNode | None" = None


BinTree = TreeNode | None


def inorder(root: BinTree) -> None: ...
def preorder(root: BinTree) -> None: ...
def postorder(root: BinTree) -> None: ...
def levelorder(root: BinTree) -> None: ...
```

要求4个函数分别按照访问顺序打印出结点的内容，格式为一个空格跟着一个字符。

## 裁判测试程序样例
```python
def main():
    # 裁判端建立树后依次调用四个遍历函数并检查输出。
    pass


if __name__ == "__main__":
    main()
```

## 输出样例
![](6-9%20二叉树的遍历/image/45.jpg)

```text
Inorder: D B E F A G H C I
Preorder: A B D F E C G H I
Postorder: D E F B H G I C A
Levelorder: A B C D F G I E H
```

## 函数部分

```python
from collections import deque


def inorder(root: BinTree) -> None:
    if root is None:
        return
    inorder(root.left)
    print(f" {root.data}", end="")
    inorder(root.right)


def preorder(root: BinTree) -> None:
    if root is None:
        return
    print(f" {root.data}", end="")
    preorder(root.left)
    preorder(root.right)


def postorder(root: BinTree) -> None:
    if root is None:
        return
    postorder(root.left)
    postorder(root.right)
    print(f" {root.data}", end="")


def levelorder(root: BinTree) -> None:
    """队列按层访问，时间 O(n)、空间 O(n)。"""
    queue = deque([root] if root is not None else [])
    while queue:
        node = queue.popleft()
        print(f" {node.data}", end="")
        if node.left is not None:
            queue.append(node.left)
        if node.right is not None:
            queue.append(node.right)
```

## 解题思路

中序、先序和后序遍历都使用递归，区别只在于访问根结点的时机：中序是“左、根、右”，先序是“根、左、右”，后序是“左、右、根”。层序遍历需要借助队列，先访问根结点，再按从左到右的顺序将孩子结点入队。四种遍历的时间复杂度均为 `O(n)`；前三种的递归栈空间为 `O(h)`，层序遍历的队列空间为 `O(n)`。

## 代码流程说明

1. 递归遍历遇到空树立即返回。
2. 中序、先序、后序函数分别在不同位置打印当前结点数据。
3. 层序遍历先将根结点入队，循环取出队首并打印。
4. 每次取出一个结点后，依次将其左、右孩子入队，队列为空时结束。

## 代码流程图

```mermaid
flowchart TD
    A[调用遍历函数] --> B{遍历类型}
    B -- 中序 --> C[左子树]
    C --> D[访问根]
    D --> E[右子树]
    B -- 先序 --> F[访问根]
    F --> G[左子树]
    G --> H[右子树]
    B -- 后序 --> I[左子树]
    I --> J[右子树]
    J --> K[访问根]
    B -- 层序 --> L[根结点入队]
    L --> M{队列是否为空}
    M -- 否 --> N[出队并访问]
    N --> O[左、右孩子依次入队]
    O --> M
```

## 解题流程图

```mermaid
flowchart TD
    A[输入二叉树] --> B[选择遍历方式]
    B --> C{是否为层序遍历}
    C -- 否 --> D[按递归访问顺序处理结点]
    C -- 是 --> E[使用队列逐层处理结点]
    D --> F[输出结点序列]
    E --> F
```
