## 6-2 顺序表操作集

- **分值：** 20分

## 题目描述

本题要求实现顺序表的操作集。

## 函数接口定义
```python
class List:
    """固定容量顺序表，data 保存有效元素，空表 data 为空。"""

    def __init__(self, max_size: int = 5) -> None:
        self.data: list[int] = []
        self.max_size = max_size


def make_empty() -> List: ...
def find(table: List, value: int) -> int: ...
def insert(table: List, value: int, position: int) -> bool: ...
def delete(table: List, position: int) -> bool: ...
```

其中`List`结构定义如下：

```python
class List:
    """固定容量顺序表，data 保存有效元素，空表 data 为空。"""

    def __init__(self, max_size: int = 5) -> None:
        self.data: list[int] = []
        self.max_size = max_size


def make_empty() -> List: ...
def find(table: List, value: int) -> int: ...
def insert(table: List, value: int, position: int) -> bool: ...
def delete(table: List, position: int) -> bool: ...
```

各个操作函数的定义为：

`List MakeEmpty()`：创建并返回一个空的线性表；

`Position Find( List L, ElementType X )`：返回线性表中X的位置。若找不到则返回ERROR；

`bool Insert( List L, ElementType X, Position P )`：将X插入在位置P并返回true。若空间已满，则打印“FULL”并返回false；否则，如果参数P指向非法位置，则打印“ILLEGAL POSITION”并返回false；

`bool Delete( List L, Position P )`：将位置P的元素删除并返回true。若参数P指向非法位置，则打印“POSITION P EMPTY”（其中P是参数值）并返回false。

## 裁判测试程序样例
```python
def main():
    # 裁判端按题面顺序调用 make_empty/find/insert/delete 并检查输出。
    pass


if __name__ == "__main__":
    main()
```

## 输入样例
```text
6
1 2 3 4 5 6
3
6 5 1
2
-1 6
```

## 输出样例
```text
FULL Insertion Error: 6 is not in.
Finding Error: 6 is not in.
5 is at position 0.
1 is at position 4.
POSITION -1 EMPTY Deletion Error.
FULL Insertion Error: 0 is not in.
POSITION 6 EMPTY Deletion Error.
FULL Insertion Error: 0 is not in.
```

## 函数部分

```python
def make_empty() -> List:
    return List()


def find(table: List, value: int) -> int:
    return table.data.index(value) if value in table.data else -1


def insert(table: List, value: int, position: int) -> bool:
    """检查容量和位置后移动元素并插入。"""
    if len(table.data) >= table.max_size:
        print("FULL", end="")
        return False
    if not 0 <= position <= len(table.data):
        print("ILLEGAL POSITION", end="")
        return False
    table.data.insert(position, value)
    return True


def delete(table: List, position: int) -> bool:
    if not 0 <= position < len(table.data):
        print(f"POSITION {position} EMPTY", end="")
        return False
    table.data.pop(position)
    return True
```

## 解题思路

顺序表用 `Last` 记录最后一个有效元素的下标，空表时令 `Last = -1`。查找从下标 0 线性扫描；插入时先判断容量和位置合法性，再从后向前移动元素；删除时从被删位置开始，将后续元素依次前移。插入和删除都必须按照对应方向移动，避免覆盖尚未处理的数据。

## 代码流程说明

1. `MakeEmpty` 分配表结构并把 `Last` 初始化为 `-1`。
2. `Find` 遍历 `[0, Last]`，找到 `X` 返回下标，否则返回 `ERROR`。
3. `Insert` 先判断是否满表，再判断 `P` 是否在 `[0, Last + 1]`；从 `Last` 到 `P` 逆序移动，写入 `X` 并更新 `Last`。
4. `Delete` 判断 `P` 是否在 `[0, Last]`；将 `P` 后面的元素前移，最后减小 `Last`。

## 代码流程图

```mermaid
flowchart TD
    A[调用操作] --> B{操作类型}
    B -- MakeEmpty --> C[分配表并令 Last=-1]
    B -- Find --> D[从 0 扫描到 Last]
    D --> E{找到 X}
    E -- 是 --> F[返回位置]
    E -- 否 --> G[返回 ERROR]
    B -- Insert --> H{表已满}
    H -- 是 --> I[输出 FULL 并失败]
    H -- 否 --> J{P 合法}
    J -- 否 --> K[输出 ILLEGAL POSITION 并失败]
    J -- 是 --> L[元素从后向前移动]
    L --> M[写入 X, Last 加一]
    B -- Delete --> N{P 合法}
    N -- 否 --> O[输出 POSITION P EMPTY]
    N -- 是 --> P[元素向前移动, Last 减一]
```

## 解题流程图

```mermaid
flowchart TD
    A[建立空顺序表] --> B[接收操作]
    B --> C{查找、插入或删除}
    C --> D[检查边界条件]
    D --> E[移动数组元素或直接扫描]
    E --> F[返回结果并输出错误信息]
    F --> B
```
